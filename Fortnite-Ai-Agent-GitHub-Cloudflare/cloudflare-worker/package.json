{
  "name": "e8-cloudflare-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test e8-admin-guard.test.mjs e8-gateway.test.mjs",
    "check": "node --check e8-admin-guard.js && node --check e8-gateway.js && node --check e8-entry.js && node --check worker.js && npm test && wrangler deploy --dry-run --outdir .wrangler-dist",
    "deploy": "npm run check && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.134.0"
  }
}
